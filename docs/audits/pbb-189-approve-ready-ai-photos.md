# PBB-189 — Approve ready AI photos during a batch

Approve is now enabled per selected photo while Perform AI continues. Every selected batch photo must have metadata from the current fixture/run and, when requested, its exact rendered After. A mixed ready/unfinished selection is blocked. The approval reserves its own busy state before asynchronous work, so background refresh cannot unlock a duplicate approval. Hide, Reject AI, source changes, new AI and upload remain protected during the batch. Normal shutdown still waits for the active AI work.

The batch report stays visible above Review with ready-to-review, still-processing, attention-required and approved counts. Separate metadata and After progress remains visible. Accepting an item does not reset the batch or let later polls restore its consumed proposal. The completion query now includes the captured fixture; previously a fixture-local metadata result could be falsely reported as needing retry.

Validation: 428 Swift tests in 32 suites pass, including a ready metadata approval while another photo is processing, duplicate activation, mixed selection, fixture-scoped proposal queries, exact rendered After/source readiness, real isolated rendition promotion during an active batch, report count partitioning, retry isolation and shutdown protection. Test approvals use isolated fixtures; no real photo is approved, regenerated or uploaded for verification.

Release identity: v265.8 / build353. Installation, signature and visible UI receipt follow below.

Installed receipt, 2026-09-22 22:59 CEST: source `14264707fb2bc6f52512e113205d5946f9fb348e`, signed Apple Development identity, strict codesign verification and embedded-runtime preview smoke passed. Installed `/Applications/PhotosByElie Backstage.app` after verifying no active metadata or visual jobs and normal idle quit. Build352 retained in the private rollback directory. Live UI shows v265.8/build353, the same selected D5H_2987 metadata and rendered After, and enabled Approve/Reject AI. Production approval and new AI generation were not exercised: concurrent behavior and report counts were verified in isolated integration tests. Standard publisher verified archive and latest manifest by R2 readback.


Follow-up regression, 2026-09-22: the operator reported roughly ten seconds before Approve enabled and missing previews after approval in build353. Reopened PBB-189. Whole-page visual configuration/status enrichment held the Review action latch; approval also invalidated thumbnails without reloading mounted rows. The queue now releases its latch once rows and selection are restored, enriches in a cancellable request-scoped task, and accepts cached exact ready results while reads continue. New mutations and fixture changes cancel stale enrichment. Unknown or unfinished After results remain blocked. Approval explicitly loads the current fixture's returned image; rows also reload on source-version changes, and cancelled thumbnail tasks cannot overwrite replacements or clear their task handles.

Validation: 428 Swift tests in 32 suites passed. Updated integration coverage checks an indefinitely held ancillary AI read leaves Review unlocked, stale refreshes cannot replace the newer receipt, cached ready results remain approvable during visual refresh, unknown results stay blocked, and real isolated After approval refills the preview without relying on row appearance. No production photos were approved, rejected, regenerated or uploaded for testing. Candidate v265.9/build354; installation evidence follows.

Installed follow-up receipt, 2026-09-22 23:14 CEST: source `c1ff689ed19b6eb67d048b334007ad5b78dc3d56`, v265.9/build354 compiled and signed; strict signature verification and embedded-runtime preview smoke passed. Standard publisher verified the archive and latest manifest by R2 readback. With no active metadata or visual jobs, normally quit build353, retained it in `Rollback/v265.8-build353-before354`, installed and launched build354. Live screenshot and accessibility inspection show the selected FullSizeRender living-room photo with rendered inspector/row previews and enabled Approve/Reject AI after initial hydration. The app restored a single first-row selection on restart, rather than the previous 57-photo selection. No production approval was performed; post-approval preview replacement and delayed-read unlocking were exercised in isolated integration tests. Computer control released.


Failed-After recovery follow-up, 2026-09-22: screenshots of D5H_3013 and D5H_3009 show generation failures, not delayed eligibility. Read-only Owner inspection confirms zero active visual jobs and 18 failed /45 ready in the original 63-photo batch. Existing metadata proposals are valid independently; the failed records have no rendered After, so Approve and Before/After must remain unavailable. The old footer incorrectly promised approval of an After anyway, and retry was session-only. Reopened the same ticket. Added an explicit approval blocker, After failed thumbnail label, and Retry After rebuilt from selected exact fixture/source receipts. It retries only failed visual work and preserves titles/keywords. The promise to approve an After appears only for a real renderable result. Ready photos retain Before/After; failed ones do not open an empty comparison. No automatic production retry or approval was performed.

Validation: isolated recovery test covers restart with no retry state, exact-source/fixture isolation, excluding successful images, duplicate suppression, and preserving metadata without a metadata pass or Review mutation. Candidate v265.10/build355.

Recovery validation result: 429 Swift tests in 32 suites passed. The unrelated immediate-H fixture test exposed its 150ms scheduling assumption twice under parallel load; replaced only that test's timed fake writer with an explicit release gate, preserving the same before-write assertion. No Gallery production behavior changed.

Installed recovery receipt, 2026-09-22 23:29 CEST: source `8ee02bc24486ddc249d8f7a40e5502e7741e36fd`, v265.10/build355 signed and verified; embedded-runtime preview smoke passed. Published archive and latest manifest through the standard R2 readback workflow. With zero active metadata/visual jobs and an idle UI, normally quit build354, retained rollback `v265.9-build354-before355`, installed and launched build355. Live screenshot and AX inspection on D5H_3009 show After failed on its thumbnail, the persistent reason beside disabled Approve, and enabled Retry After; ready D5H_3006 retains Before/After. The misleading promise to approve a nonexistent After is absent. No production retry/approval/upload was initiated. User selection changed during live inspection; left the selected failed photo intact. Computer control released.


Parallel After queue follow-up, 2026-09-22: replace the single global generator lock and 600-second queue expiry with three cross-process generation slots. Queued workers retain a 30-second heartbeat and check cancellation while waiting; waiting has no generation deadline. Per-proposal locks prevent duplicate provider work; process exit releases slots automatically. Shared compatibility locking respects older active workers during upgrades. The provider request keeps its existing 300-second timeout and does not automatically retry uncertain outcomes. Review polling now bounds a sustained loss of status instead of timing out a healthy long queue. Exact fixture/source validation, immutable originals, and explicit approval/upload boundaries are unchanged.

Validation: 429 Swift tests across 32 suites pass; 33 Python tests cover parallel admission, capacity enforcement, process death, duplicate suppression, long queue residence, cancellation, heartbeat/expiry interaction, real synthetic draft materialization and fixture/source invariants, plus runtime packaging. Updated the packaging test fixture to include the three already-required fixture-edition modules. Candidate v265.11/build356. No production AI request or approval was run for validation.
