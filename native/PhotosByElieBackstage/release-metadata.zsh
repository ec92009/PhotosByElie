# Shared release metadata for the Backstage operator app and its signed
# Photos Bridge helper. Keep these values in lockstep for one distributable
# release; the legacy SIDECAR_VERSION file belongs to the retired Sidecar
# surface and must not drive either native bundle.

PBE_BACKSTAGE_BUNDLE_IDENTIFIER="com.photosbyelie.backstage"
PBE_BACKSTAGE_VERSION="218.0"
PBE_BACKSTAGE_BUILD="75"

PBE_PHOTOS_BRIDGE_BUNDLE_IDENTIFIER="com.photosbyelie.photos-bridge"
PBE_PHOTOS_BRIDGE_VERSION="218.0"
PBE_PHOTOS_BRIDGE_BUILD="75"
